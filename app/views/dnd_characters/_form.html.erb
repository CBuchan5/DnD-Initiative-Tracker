<%= form_with(model: dnd_character) do |form| %>
  <% if dnd_character.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(dnd_character.errors.count, "error") %> prohibited this dnd_character from being saved:</h2>

      <ul>
        <% dnd_character.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :character_name, style: "display: block" %>
    <%= form.text_field :character_name %>
  </div>

  <div>
    <%= form.label :character_class, style: "display: block" %>
    <%= form.text_field :character_class %>
  </div>

  <div>
    <%= form.label :initiative, style: "display: block" %>
    <%= form.number_field :initiative %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
